# homebrew-tap

Homebrew tap for huyz0's tools.

```bash
brew install huyz0/tap/lsp-cli
```

Formulas here are kept up to date automatically by each tool's own release
workflow — do not edit them by hand, changes will be overwritten on the
next release.
